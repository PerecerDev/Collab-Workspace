# Task Reports

Agents write JSON task reports here on completion: `<agent>-task-report.json`

See `AGENT_OPERATING_SYSTEM.md` for schema expectations.

PM reads `persisted_data` before next delegation. Clean up after delivery (keep this README).
